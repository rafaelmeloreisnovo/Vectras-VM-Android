# RAFCODEphi Code Entry Map — Vectras-VM-Android

## Purpose

This document is a technical entry map for reading the repository from the code path, not from legacy documentation alone.

The documentation may be treated as an older map. The code path is the operational route.

## Core claim

`Vectras-VM-Android` represents the Android VM execution leg of the RAFCODEphi / RAFAELIA / VECTRA workbench.

The defensible technical description is:

> Android VM runtime and integration layer using Java/Kotlin, Gradle, JNI/native acceleration, QEMU argument construction, fallback behavior, and RAFAELIA/RMR-related native modules