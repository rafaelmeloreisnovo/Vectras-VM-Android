org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
#org.gradle.daemon=false
# SDK_VERSION changed from 21 to 17 for CI compatibility with GitHub Actions Java 17 runtime
# This doesn't impact the Android minSdk/targetSdk which are configured separately in build.gradle
SDK_VERSION=17
android.useAndroidX=true