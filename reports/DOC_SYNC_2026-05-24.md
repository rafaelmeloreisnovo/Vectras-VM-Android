# DOC_SYNC_2026-05-24

## Objetivo
Atualizar a governança documental com foco em **determinismo**, rastreabilidade e redução de documentação solta/órfã.

## Ciclo 1 — Inventário determinístico (snapshot)
Data do snapshot: **2026-05-24**.

### Comandos usados
```bash
rg --files -g '*.md'
rg --files -g '*.md' | wc -l
rg --files -g '*.md' | awk -F/ 'NF==1 {print}' | sort
```

### Resultado
- Total de arquivos Markdown no repositório no momento do snapshot: **366**.
- Arquivos Markdown na raiz (NF==1):
  - `AGENTS.md`
  - `BUILDING.md`
  - `CHANGELOG.md`
  - `COMPILATION_FIXES.md`
  - `CONTRIBUTING.md`
  - `DOC_INDEX.md`
  - `FIXES_SUMMARY.md`
  - `LICENSE.md`
  - `PRIVACY.md`
  - `PROJECT_STATE.md`
  - `README.md`
  - `RELEASE_NOTES.md`
  - `START_HERE.md`
  - `THIRD_PARTY_NOTICES.md`
  - `TROUBLESHOOTING.md`
  - `VERSION_STABILITY.md`
  - `VECTRA_CORE.md`
  - `VECTRAS_MEGAPROMPT_DOCS.md`
  - `boot2exem.md`
  - `qtm.md`
  - `rafael.md`
  - `raiz de cinco por doze.md`

## Ciclo 2 — Consolidação de navegação

### 2.1 Estado operacional
- `PROJECT_STATE.md` permanece como fonte de estado de ciclo.
- Este relatório passa a ser o **registro canônico desta rodada de sincronização documental**.

### 2.2 Cadeia de entrada (start path)
- `START_HERE.md` aponta explicitamente para este relatório e reforça a trilha de validação de documentação antes de build/test.

### 2.3 Invariantes mantidas
- Sem alteração em contratos matemáticos declarados de período/atratores.
- Sem alteração em assembly/registradores (x0..x4).
- Sem alteração de caminhos de build canônicos.

## Gaps (abertos)
1. Existem arquivos Markdown na raiz com semântica histórica/experimental que ainda pedem realocação para domínio (`archive/`, `docs/active/`, `reports/`) sem quebrar links.
2. Não foi executado build Android nesta rodada por ausência de SDK local (`ANDROID_SDK_ROOT`/`ANDROID_HOME`).
3. Não foi executada validação "bitomega.log → period-42 confirmed" nesta rodada por depender do ambiente Android/NDK configurado.

## Política de próximos passos (determinística)
1. Classificar cada `.md` da raiz em: `ativo`, `histórico`, `experimental`, `evidência`.
2. Mover por lote pequeno (<=5 arquivos por commit), atualizando links no mesmo commit.
3. Rodar `./run_tests.sh` após setup do SDK e registrar saída em novo relatório datado.
4. Atualizar `DOC_INDEX.md` a cada novo movimento de documento de raiz.

## Resumo executivo
- Esta rodada executou **sincronização de referência** (inventário + cadeia de navegação + gaps explícitos), sem tocar runtime, sem tocar ABI, sem tocar módulos low-level.
